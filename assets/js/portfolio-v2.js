(()=>{const header=document.querySelector('[data-header]'),toggle=document.querySelector('.toggle'),links=document.querySelector('.links'),reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const head=()=>header.classList.toggle('scrolled',scrollY>20);head();addEventListener('scroll',head,{passive:true});toggle.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));toggle.classList.toggle('open',!open);links.classList.toggle('open',!open);document.body.classList.toggle('menu-open',!open)});links.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{toggle.setAttribute('aria-expanded','false');toggle.classList.remove('open');links.classList.remove('open');document.body.classList.remove('menu-open')}));document.querySelectorAll('[data-year]').forEach(el=>el.textContent=new Date().getFullYear());document.querySelectorAll('.filter').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const filter=btn.dataset.filter;document.querySelectorAll('.project').forEach(card=>card.classList.toggle('hidden',filter!=='all'&&!card.dataset.category.split(' ').includes(filter)))}));const reveals=document.querySelectorAll('[data-reveal]');if(reduced)reveals.forEach(el=>el.classList.add('visible'));else{const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12});reveals.forEach(el=>observer.observe(el))}const sections=[...document.querySelectorAll('main section[id]')],nav=[...links.querySelectorAll('a[href^="#"]')];const active=()=>{let id='';sections.forEach(s=>{if(scrollY>=s.offsetTop-180)id=s.id});nav.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+id))};active();addEventListener('scroll',active,{passive:true})})();
